var people = ["Fred", "Jimbo", "Cornelius", "Pepperjack"];
var numbers = [12, 76, 52, 33];
var goodRadioSongs = [false, true, false, false];

people.push()
console.log(people)

numbers.shift()
console.log(numbers)

goodRadioSongs.pop()
console.log(goodRadioSongs)

// The index position is the position of the values' position within a variable.
// Index positions start at 0 rather than 1. 
// An example would be identifing 76 in the numbers variable as index position 1,
// since 12 would be 0 intead of itself as 1 under normal contexts outside of Javascript.